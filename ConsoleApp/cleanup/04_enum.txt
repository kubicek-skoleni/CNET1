using System.ComponentModel;
using System.Reflection;

Console.WriteLine("Zadej èíslo 1-7 a zmáèkni enter:");

string? line = Console.ReadLine();

int dayNumber = int.Parse(line);

if(dayNumber < 1 || dayNumber > 7)
{
    Console.WriteLine("Èíslo musí být v rozsahu 1-7.");
    return;
}

DnyvTydnu den = (DnyvTydnu)dayNumber;

Console.WriteLine($"Den v týdnu: {den}");


enum DnyvTydnu
{
    [Description("Pondìlí")]
    PONDELI = 1,

    [Description("úterý")]
    UTERY = 2,

    [Description("støeda")]
    STREDA = 3,
    
    CTVRTEK = 4,
    PATEK = 5,
    SOBOTA = 6,
    NEDELE = 7
}
